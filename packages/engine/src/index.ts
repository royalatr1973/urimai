/**
 * @urimai/engine — the deterministic eligibility rules engine.
 *
 * The product's moat. Pure, no I/O, fully unit-testable. The LLM never decides;
 * it only produces a Profile (Phase 2). Verdicts come from here.
 */
export { evaluate } from "./evaluate.js";
